public class Main {
    //TODO: Declare the Employee array staff.
    private Employee [] employees;
    //      Declare the variable numberOfRecords to hold the current number of records in the array.
    private int numberOfRecords;
    

   public static void main(String[] args) {
        /* TODO: Instantiate an array of maximum capacity 100 and assign it to staff.
         *       Initialize numberOfRecords to 0.
         */
        Employee [] employees = new Employee [100];
        int numberOfRecords = 0;
        /* TODO: read the file and load the array with the employee records.
         *       display the original array
         *       sort the array
         *       display the sorted array
         */
        
        //         try (loadEmployeesFromFile (filename))
        //         {
        //         }
    }
    
    /* TODO: open the file and read all the records. Load the array staff. */
    private static void loadEmployeesFromFile(String filename) {
        
    }// end loadEmployeesFromFile()
   
    /* TODO: sort the array staff using selection sort. */
    private static void sortEmployees() {
        
    }// end sortEmployees()
    
    /* TODO: swaps the two elements indexed by the parameters. */
    private static void swap(int index1, int index2) {
        
    }// end swap()
    
    /* TODO: returns the index of the smallest element in ascending order, starting at the specified index. */
    private static int indexOfSmallestElement(int startingAt) {
        return 1;
        
    }// end indexOfSmallestElement()
    
    /* TODO: displays the label, followed by all the employee records in the staff array. */
    private static void displayEmployees(String label) {
        
    }// end displayEmployees()
}
